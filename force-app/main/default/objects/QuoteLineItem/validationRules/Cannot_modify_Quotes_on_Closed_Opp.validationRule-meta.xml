<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cannot_modify_Quotes_on_Closed_Opp</fullName>
    <active>true</active>
    <description>Prevents users from editing Quote Line Items after an Opportunity is Closed.</description>
    <errorConditionFormula>AND($User.ProfileId &lt;&gt; &apos;00e6g0000027lpb&apos;,

OR(ISPICKVAL(Quote.Opportunity.StageName, &quot;Send to Order Management&quot;), 
	ISPICKVAL(Quote.Opportunity.StageName, &quot;Closed Won&quot;), 
	ISPICKVAL(Quote.Opportunity.StageName, &quot;Closed Lost&quot;))

)</errorConditionFormula>
    <errorMessage>Quote Line Items cannot be created or edited because the Opportunity is Closed.</errorMessage>
</ValidationRule>
